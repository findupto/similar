const KEY='mkpos:online-config';const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}};export const OnlineBridge={getConfig:read,saveConfig(p){const n={...read(),...p};localStorage.setItem(KEY,JSON.stringify(n));return n},async request(path,options={}){const c=read();if(!c.apiUrl)throw new Error('Online ordering API is not configured');const r=await fetch(c.apiUrl.replace(/\/$/,'')+path,{...options,headers:{'Content-Type':'application/json',...(c.token?{Authorization:'Bearer '+c.token}:{}),...(options.headers||{})}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Online service error');return d},async publishMenu(state){const c=read();return this.request('/api/menu',{method:'PUT',body:JSON.stringify({menuId:c.menuId||'default',settings:state.settings,products:state.products.filter(p=>p.active!==false)})})},async getNewOrders(){return this.request('/api/orders?status=NEW')},async setStatus(id,status){return this.request('/api/orders/'+encodeURIComponent(id),{method:'PATCH',body:JSON.stringify({status})})}};