import './salesAudit';
import {logPrint} from './printHistory';
const KEY='mkpos:printer-config';
export const RECEIPT_THEMES={modern:{name:'Modern Professional',width:24},classic:{name:'Classic Restaurant',width:24},minimal:{name:'Minimal Clean',width:24},kitchen:{name:'Kitchen Bold',width:24},compact:{name:'Compact Clean',width:24}};
const PROFILE={model:'YD801',version:'YC1.12.03 2026-04-22HBb',dots:192,font:'12x24',codepage:'GB2312',baudRate:460800,heatTime:'950,150',heatDensity:'Normal',feedLine:'LF (0A)',bluetoothName:'B11-70604273',bluetoothAddress:'66:22:BE:5B:40:F6',bluetoothVersion:'583-B',maxFeed:'20mm'};
const cfg=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'{}')}catch{return{}}};
const text=(v='')=>String(v).replace(/[\r\n]+/g,' ').trim(),cut=(v,w)=>text(v).slice(0,w),pad=(v,w)=>text(v).slice(0,w).padEnd(w),right=(v,w)=>text(v).slice(0,w).padStart(w),line=(w=24)=>'-'.repeat(w)+'\n',money=v=>Number(v||0).toFixed(2);
const E={init:'\x1b\x40',left:'\x1b\x61\x00',center:'\x1b\x61\x01',bold:'\x1b\x45\x01',normal:'\x1b\x45\x00',big:'\x1b\x21\x10',normalSize:'\x1b\x21\x00',feed:'\x1b\x64\x03'};
const normalizeEsc=v=>String(v||'').replace(/\\x([0-9a-fA-F]{2})/g,(_,h)=>String.fromCharCode(parseInt(h,16))).replace(/\\n/g,'\n').replace(/\\r/g,'\r');
const encode=v=>Array.from(new TextEncoder().encode(normalizeEsc(v).replace(/[^\x00-\x7F]/g,'?')));
function header(business,address,phone){let o=E.init+E.center+E.bold+E.big+cut(business,24)+'\r\n'+E.normal+E.normalSize;if(address)o+=cut(address,24)+'\r\n';if(phone)o+=cut(phone,24)+'\r\n';return o+E.left}
function itemsBlock(items){let o='';for(const i of items||[]){const qty=String(i.qty??1),name=cut(i.variant?`${i.name} · ${i.variant}`:i.name,17),amt=money(i.total??Number(i.price||0)*Number(i.qty||1));o+=pad(qty+' x '+name,17)+right(amt,7)+'\r\n'}return o}
function receipt({business,address,phone,invoice,date,customer,orderType,info,items,subtotal,discount=0,total,method,paid,change,footer='Thank you for your order!'}){let o=header(business,address,phone);o+=E.center+E.bold+cut(orderType||'ORDER',24)+'\r\n'+E.normal+E.normalSize+E.left+line();o+=pad('Invoice:',16)+right(invoice,8)+'\r\n'+pad('Date:',16)+right(date,8)+'\r\n';if(customer)o+=pad('Customer:',16)+right(cut(customer,8),8)+'\r\n';if(info)o+=pad('Info:',16)+right(cut(info,8),8)+'\r\n';o+=line()+pad('QTY ITEM',17)+right('AMT',7)+'\r\n'+line()+itemsBlock(items)+line()+pad('Subtotal',17)+right(money(subtotal),7)+'\r\n'+pad('Discount',17)+right(money(discount),7)+'\r\n'+E.bold+pad('TOTAL',17)+right(money(total),7)+'\r\n'+E.normal+line()+pad('Payment',17)+right(method||'',7)+'\r\n'+pad('Paid',17)+right(money(paid),7)+'\r\n'+pad('Change',17)+right(money(change),7)+'\r\n'+line()+E.center+E.bold+cut(footer,24)+'\r\n'+E.normal+E.feed;return normalizeEsc(o)}
function kitchen({business,address,invoice,date,customer,orderType,info,items,footer='PREPARE ORDER'}){let o=header(business,address,'');o+=E.center+E.bold+cut(orderType||'ORDER',24)+'\r\n'+E.normal+E.normalSize+E.left+line()+pad('ORDER #',16)+right(invoice,8)+'\r\n'+pad('TIME',16)+right(date,8)+'\r\n';if(customer)o+=pad('CUSTOMER',16)+right(cut(customer,8),8)+'\r\n';if(info)o+=pad('INFO',16)+right(cut(info,8),8)+'\r\n';o+=line()+E.bold+E.big+'ITEMS TO PREPARE\r\n'+E.normal+E.normalSize+line();for(const i of items||[])o+=E.bold+E.big+String(i.qty??1).padStart(2)+' x '+cut(i.variant?`${i.name} · ${i.variant}`:i.name,21)+'\r\n'+E.normal+E.normalSize+'\r\n';return normalizeEsc(o+line()+E.center+E.bold+footer+'\r\n'+E.normal+E.feed)}
async function send(data,printer){if(!printer)return{ok:false,skipped:true,error:'No printer selected'};const bytes=encode(data),address=printer.bluetoothAddress||PROFILE.bluetoothAddress,port=String(printer.transportPort||printer.port||'').trim(),printerName=String(printer.printerName||printer.windowsQueue||'').trim();if(window.mkPosDesktop?.printEscPos){return window.mkPosDesktop.printEscPos({address,data:bytes,port,baudRate:Number(printer.baudRate)||PROFILE.baudRate,printerName,transport:printer.transport==='serial'?'serial':'auto'});}return{ok:false,error:'Desktop printer transport unavailable'};}
export const PrinterBridge={
 async discover(){const base={id:PROFILE.bluetoothAddress,name:PROFILE.bluetoothName,description:'YD801 · Bluetooth SPP',port:'Bluetooth',transportPort:'',transport:'auto',printerName:'',bluetoothAddress:PROFILE.bluetoothAddress,bluetoothName:PROFILE.bluetoothName,profile:PROFILE,baudRate:PROFILE.baudRate};try{const p=window.mkPosDesktop?.discoverPrinters?await window.mkPosDesktop.discoverPrinters():[];const all=[base,...p];const seen=new Set();return all.filter(x=>{const k=`${x.type||''}|${x.port||''}|${x.id||''}`;if(seen.has(k))return false;seen.add(k);return true});}catch{return[base]}},
 async reconnect(){const c=cfg();return c.customer||c.kitchen||null},
 async connect(printer,role='customer'){const v={...printer,role,name:printer?.name||'YD801 · Bluetooth',description:'YD801 Thermal Printer · Bluetooth SPP',bluetoothAddress:printer?.bluetoothAddress||PROFILE.bluetoothAddress,bluetoothName:printer?.bluetoothName||PROFILE.bluetoothName,baudRate:Number(printer?.baudRate)||PROFILE.baudRate,transport:printer?.transport||'auto',transportPort:printer?.transportPort||printer?.port||'',port:printer?.port||'Bluetooth SPP',profile:PROFILE};const c=cfg();c[role]=v;localStorage.setItem(KEY,JSON.stringify(c));return v},
 async disconnect(role){const c=cfg();delete c[role];localStorage.setItem(KEY,JSON.stringify(c));return c},
 async test(printer){const p=printer||cfg().customer||{bluetoothAddress:PROFILE.bluetoothAddress,transport:'auto'};if(!window.mkPosDesktop?.testSerial)return{ok:false,error:'Desktop printer transport unavailable'};return window.mkPosDesktop.testSerial({address:p.bluetoothAddress||PROFILE.bluetoothAddress,data:encode(E.init+'MK PIZZA POS\r\nYD801 BLUETOOTH TEST\r\n\r\n'),port:p.transportPort||(/^COM\d+$/i.test(p.port||'')?p.port:''),baudRate:Number(p.baudRate)||PROFILE.baudRate,printerName:p.printerName||'',allPorts:true});},
 async print(r,p){const c=cfg(),x=p||c.customer,out=await send(receipt({...r,footer:c.footer||'Thank you for your order!'}),x);if(out?.ok)logPrint('customer',r);return out},
 async printCustomer(r,p){return this.print(r,p)},
 async printKitchen(o,p){const c=cfg(),x=p||c.kitchen,out=await send(kitchen({...o,footer:c.kitchenFooter||'PREPARE ORDER'}),x);if(out?.ok)logPrint('kitchen',o);return out},
 saveConfig(patch){const n={...cfg(),...patch};localStorage.setItem(KEY,JSON.stringify(n));return n},
 getConfig(){return cfg()},
 getProfile(){return PROFILE},
 setTheme(theme){return this.saveConfig({theme:RECEIPT_THEMES[theme]?theme:'modern'}).theme},
 getTheme(){return cfg().theme||'modern'}
};
